- if @project.pages_deployed?
  - if can?(current_user, :remove_pages, @project)
    .card.border-danger
      .card-header.bg-danger.text-white
        = s_('GitRepoPages|Remove pages')
      .errors-holder
      .card-body
        %p.gl-mb-0
          = s_('GitRepoPages|Removing pages will prevent them from being exposed to the outside world.')
      .card-footer
        = link_to s_('GitRepoPages|Remove pages'), project_pages_path(@project), data: { confirm: s_('GitRepoPages|Are you sure?'), 'confirm-btn-variant': 'danger'}, method: :delete, class: "btn gl-button btn-danger", "aria-label": s_('GitRepoPages|Remove pages')
  - else
    .nothing-here-block
      = s_('GitRepoPages|Only project maintainers can remove pages')
